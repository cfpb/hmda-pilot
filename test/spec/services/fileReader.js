'use strict';

require('../../../app/scripts/services/fileReader');

describe('Service: FileReader', function () {

    var service;

    // Initialize the service
    beforeEach(function () {
        service = new FileReader();
    });

    describe('readFile', function() {
        it('should return the contents of a file as text', function () {});
    });
});
